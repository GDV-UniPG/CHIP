import { TransportMeanPipe } from './transport-mean.pipe';

describe('TransportMeanPipe', () => {
  it('create an instance', () => {
    const pipe = new TransportMeanPipe();
    expect(pipe).toBeTruthy();
  });
});
